<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryDark">@color/primaryColorDark</item>
        <item name="colorAccent">@color/accentColor</item>
        <!-- these only work in lollipop. but do they work in appcompactactivity?  NO.
        <item name="textColorPrimary">@color/textColor</item>
        <item name="textColorSecondary">@color/textColorSecondary</item>

        <item name="navigationBarColor">@color/navigationBarColor</item>
        <item name="windowBackground">@color/windowBackgroundColor</item>

         -->
    </style>

</resources>
